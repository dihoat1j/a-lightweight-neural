[project]
name = "a_lightweight_neural"
version = "0.1.0"
description = "Production-ready a-lightweight-neural toolkit for multi-agent orchestration and shared memory."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
